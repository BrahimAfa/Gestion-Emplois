@model GestionEmploi.Models.Semaine
@{
    ViewBag.Title = "SemaineList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<br />
<h2>Liste des semaines </h2>

@*<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#sem">Ajouter une Semaine</button><br />

    <div class="modal fade" id="sem" role="dialog" aria-labelledby="ModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h2 class="modal-title">Week Information</h2>
                </div>
                <div class="modal-body">
                    @using (Html.BeginForm("SemaineAdd", "Semaine", FormMethod.Post))
                    {
                        <div class="container">
                            <div class="form-group">
                                @if (Model != null && Model.SemaineId > 0)
                                {
                                    @Html.HiddenFor(x => x.SemaineId)
                                }
                                <label>Libelle</label>
                                @Html.TextBoxFor(x => x.libelle, new { @class = "form-control" })
                            </div>
                            <div class="form-group">
                                <label>Date de début</label>
                                @Html.EditorFor(x => x.DateDebut, new { htmlAttributes = new { @class = "form-control" } })
                                @Html.ValidationMessageFor(x => x.DateDebut, "", new { @class = "text-danger" })
                            </div>
                            <div class="form-group">
                                <label>Date de fin</label>
                                @Html.EditorFor(x => x.DateFin, new { htmlAttributes = new { @class = "form-control" } })
                                @Html.ValidationMessageFor(x => x.DateFin, "", new { @class = "text-danger" })
                            </div>

                            <input type="submit" value="Ajouter" class="btn btn-primary" />
                            <input type="reset" value="cancel" class="btn btn-danger" />
                        </div>
                    }
                    @section Scripts{
                        @Scripts.Render("~/bundles/jqueryui")
                        @Styles.Render("~/Content/cssjqryUi")

                        <script type="text/javascript">

                            $(document).ready(function () {
                                $('input[type=datetime]').datepicker({
                                    dateFormat: "dd/M/yy",
                                    changeMonth: true,
                                    changeYear: true,
                                    yearRange: "-60:+0"
                                });

                            });
                        </script>
                        <script src="@Url.Content("~/Scripts/jquery.validate.min.js")"
                                type="text/javascript">
                        </script>

                        <script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")"
                                type="text/javascript">
                        </script>
                    }
                </div>
            </div>
        </div>

    </div>*@




<br />
<a href="@Url.Action("Semaine")" class="btn btn-primary" style="margin:400 0 0 0;">Ajouter une Semaine</a>
<br />
<br />
<a href="@Url.Action("Semainepdf")" class="btn btn-primary" style="margin:400 0 0 0;">Voir l'emploi</a>
<br />
<br />

<table class="table table-hover">
    <tr class="btn-primary">
        <th>ID Semaine</th>
        <th>Libellé</th>
        <th>Date de début</th>
        <th>Date de fin</th>
        <th>Action</th>
    </tr>
    @foreach (var item in ViewBag.list)
    {
        <tr>
            <td>@item.SemaineId</td>
            <td>@item.libelle</td>
            <td>@item.DateDebut</td>
            <td>@item.DateFin</td>
            <td>
                <a href="@Url.Action("Semaine",new { item.SemaineId,item.libelle,item.DateDebut,item.DateFin})" class="btn btn-success">Edit</a>
                <a href="@Url.Action("Delete",new { id = item.SemaineId})" class="btn btn-danger">Delete</a>
            </td>
        </tr>
    }
</table>




